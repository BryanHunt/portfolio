import { formatCsv } from '../../../helpers/format-csv';
import { module, test } from 'qunit';

module('Unit | Helper | format csv');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatCsv([42]);
  assert.ok(result);
});
