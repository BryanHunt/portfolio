{{#paper-toolbar}}
  <h2 class="md-toolbar-tools">
    <span>Ember Split View</span>
  </h2>
{{/paper-toolbar}}

{{#paper-content classNames="md-padding"}}
  <p>
    The <a href="https://github.com/BryanHunt/ember-split-view">ember-split-view</a>
    addon allows you to split a view into two views separated by a movable sash.
    Dragging the sash resizes one view relative to the other.  Split views can
    be nested to creat a composite as demonstrated below.
  </p>
  <p>
    {{#split-view isVertical=true splitPercentage=30 class="splitViewVertical"}}
      {{split-child class="border"}}
      {{split-sash}}
      {{#split-child}}
        {{#split-view isVertical=false}}
          {{split-child class="border"}}
          {{split-sash}}
          {{split-child class="border"}}
        {{/split-view}}
      {{/split-child}}
    {{/split-view}}
  </p>
  <h3>Template</h3>
  <pre>
  \{{#split-view isVertical=true splitPercentage=30 class="splitViewVertical"}}
    \{{split-child class="border"}}
    \{{split-sash}}
    \{{#split-child}}
      \{{#split-view isVertical=false}}
        \{{split-child class="border"}}
        \{{split-sash}}
        \{{split-child class="border"}}
      \{{/split-view}}
    \{{/split-child}}
  \{{/split-view}}
  </pre>
{{/paper-content}}
